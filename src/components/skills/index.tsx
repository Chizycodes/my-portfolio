'use client';
import { Title } from '../common/Title';
import data from '@/data';
import React from 'react';

export const Skills = () => {
	return (
		<div className="" id="skills">
			<Title title="Skills" />
		</div>
	);
};
